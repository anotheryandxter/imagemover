# FileOrganizer DMG Release

This branch contains a distributable DMG for FileOrganizer v1.0.0.

Files included:

- `FileOrganizer-1.0.0.dmg` — mountable installer for macOS (drag FileOrganizer.app to Applications)

Notes:

- The DMG is built from the latest code and includes the new "Export Session" feature.
- For wider distribution, consider code-signing and notarization.

Installation:

1. Double-click `FileOrganizer-1.0.0.dmg` to mount.
2. Drag `FileOrganizer.app` to the `Applications` shortcut inside the DMG.
3. Eject the DMG and run the app from `Applications`.

